# CI/CD Foundation

GitHub Actions is prepared as a quality pipeline, not as an automatic production
deployment pipeline. It validates Python 3.12 and 3.13, installs development
dependencies, runs static analysis, tests, coverage, and compilation.

Production deployment remains intentionally out of scope for Phase 0.
