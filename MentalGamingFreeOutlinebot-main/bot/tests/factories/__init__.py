"""
Object factories for generating realistic test data at scale.

Each factory uses ``faker`` for realistic values and ``dataclasses`` for structure.
Import examples:
    from tests.factories.user_factory import UserFactory
    from tests.factories.wallet_factory import WalletFactory
"""
